"use client";
import { Modal } from "@/components/ui/modal";
// import { UserButton } from "@clerk/nextjs";

export default function SetupPage() {
  return (
    <div>
      asd
      <Modal title="title" description="des" isOpen onClose={() => {}}>
        child
      </Modal>
      sdf
    </div>
  );
}
